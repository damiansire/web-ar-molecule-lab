{
  "name": "web-ar-molecule-lab",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  },
  "description": "Interactive molecule laboratory using WebGPU and hand-tracking for augmented reality in the browser."
}